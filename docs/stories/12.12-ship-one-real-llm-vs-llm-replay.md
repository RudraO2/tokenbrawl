---
story: "12.12"
epic: E12
status: ready-for-dev
frs: []
depends_on: ["12.1", "12.11"]
operator_actions:
  - "Provide GROQ_API_KEY in the environment of the machine running the generator. A free-tier Groq key satisfies INV-8; the story's generator reads it from the environment only and never writes it to a file (packages/cli/src/secrets.ts). This is the single blocking item — every other acceptance criterion below is completed and committed by the dev session."
  - "Optionally provide CEREBRAS_API_KEY and/or GOOGLE_AI_STUDIO_API_KEY as well, if the shipped replay should be a cross-provider fight rather than two Groq deployments. Not required: one key is enough to ship the story."
  - "After the key is available, run the committed generator (its exact command line is recorded in this story's completion note), review the produced Command Log and reasoning sidecar for anything a model said that should not ship, then commit both and run `bmad-loop confirm 12-12-ship-one-real-llm-vs-llm-replay`."
---

# Story 12.12 — Ship one real fight between two language models

As a visitor, I want the flagship replay to be two actual language models fighting with their real reasoning attached, so that the thing this project is *for* is on the screen instead of two Baseline Bots standing in for it.

**This story parks at `awaiting-operator` and must not block the run.** Its blocking input is a provider API key, which no agent can self-serve. `.bmad-loop/policy.toml` sets `operator.enabled = true` precisely for this shape: the agent-doable work is finished and committed, the run moves on, and what is owed is recorded in this file's `operator_actions:` frontmatter above. Do not stub past it, do not fake a log, and do not mark it done.

## Why this is the last story in the epic

Everything above it makes the product look like an arcade fighter. This one makes it be what it claims to be.

The claim is on the page: *"language models fighting under a fixed token budget"* (`apps/web/index.html`'s description meta). What ships is a corpus of Baseline-Bot-versus-Baseline-Bot Matches. Every `decisions[]` entry in every committed log reads:

```json
{ "reasoning": null, "rawResponse": "random:advance", "provider": "bot", "endpoint": "bot" }
```

`reasoning: null` on every decision of every log. Story 4.3's hover reasoning is described in `docs/stories/README.md` as the **centrepiece feature**, and there is no committed replay in which it has anything to show. A visitor hovering a Decision Point today gets nothing, on the feature the project's own story queue calls its centre.

Everything needed to produce a real one already exists and is shipped: Story 3.1's Deployment Agent, Story 3.2's Groq adapter, Story 3.3's Cerebras and Google adapters, Story 3.4's metering probe, Story 3.5's provenance and prompt caching, Story 4.3's reasoning sidecar and Story 5.1's local CLI. Nothing here is new engineering. The missing input is a key.

## What the dev session does before parking

All of it, committed, so the operator's job is to run one command and review its output:

1. **A committed generator** — a script beside `apps/web/scripts/build-spectate-manifest.mts` that runs one Deployment-versus-Deployment Match through the existing CLI path, writes the Command Log and its reasoning sidecar into `apps/web/public/replays/`, and updates the manifest. Deterministic in everything except the model's own responses, which are recorded in the log and are what makes the replay a replay (INV-2: the Match is re-simulated from the log, never recorded).
2. **Free-tier only.** `assertFreeTierEndpoint` governs tournament configuration and is not loosened. INV-8 is zero recurring cost, and a free-tier Groq key satisfies it. The generator refuses a non-allowlisted endpoint rather than warning about one.
3. **Key hygiene, proven.** The key is read from the environment only (`packages/cli/src/config.ts` rejects a literal key in config and says so), never written to a file, never echoed. `scripts/assert-no-secret-leak.sh` and `packages/cli/src/secrets.test.ts` cover this and must pass over the generator and over its output.
4. **The failure path is written and tested first.** With no key present the generator exits with a clear message naming `GROQ_API_KEY` — which is the state it will actually be in when the dev session runs it, and therefore the only behaviour the dev session can verify end to end.
5. **The player is proven ready for the log it cannot yet produce**, against a fixture: a synthetic v2 log with a populated sidecar, asserting that hover reasoning renders real text, that `provider` and `endpoint` show on the Decision Point, that the Token Bank HUD draws a real budget, and that a Parse Failure in the corpus renders as a Parse Failure rather than as a gap. Every one of these paths ships untested against real data today.
6. **The rating exclusion is correct.** A single exhibition replay is not a tournament result. It must not enter the leaderboard by existing — `ratingEligibility` decides, and the story asserts the answer rather than assuming it.

## Acceptance criteria

**Given** no provider key in the environment, **When** the generator runs, **Then** it exits non-zero naming `GROQ_API_KEY` exactly, writes no file and leaks no secret. This is the AC the dev session verifies.

**Given** a fixture v2 Command Log with a populated reasoning sidecar, **When** the replay player mounts it, **Then** hovering a Decision Point shows that decision's real reasoning text, and the Decision Point shows its `provider` and `endpoint` — the check `reasoning-panel-shows-text`, added to `scripts/visual-gate.mjs` by this story, which hovers a Decision Point on `#app` and requires non-empty text in the reasoning panel. Run against the fixture until the real log lands, and against the real log afterwards.

**Given** the fixture, **When** the Token Bank HUD draws, **Then** it shows a real budget rather than the empty meter a Baseline Bot produces. Same gate run.

**Given** a Parse Failure in the fixture, **When** it is rendered, **Then** it reads as a Parse Failure in `--tb-warn` treatment — warning text set in `--tb-bg` ink on a red fill, because `--tb-warn` on `--tb-bg` measures 4.26:1 and does not clear the text floor (`docs/DESIGN.md`). Parse Failures are a first-class metric in `docs/INVARIANTS.md` and this is the only surface a visitor meets one on.

**Given** the shipped exhibition replay, **When** `ratingEligibility` is asked, **Then** it is excluded from the leaderboard, asserted rather than assumed.

**Given** a provider key **(operator)**, **When** the generator runs, **Then** it produces one v2 Command Log with real `provider`, `endpoint` and per-decision `reasoning`, plus its sidecar, and the log verifies its own Final-State Hash on replay.

**Given** that replay **(operator)**, **When** a visitor loads the site, **Then** it is the flagship replay the player opens on, and `reasoning-panel-shows-text` passes against real model output.

**Given** the repository at any point, **When** `scripts/assert-no-secret-leak.sh` runs, **Then** it passes. No key in a log, a sidecar, a manifest, a screenshot or a report.

## Scope

A new generator script under `apps/web/scripts/` or `packages/cli/src/`, a fixture log and sidecar under `apps/web/src/testing/`, `apps/web/src/replay/sidecar.ts` and the reasoning panel **only** if the fixture exposes a defect in them, `scripts/visual-gate.mjs` (`reasoning-panel-shows-text`), `docs/ASSETS.md` if the replay is recorded there.

Not in scope: any change to a provider adapter, to `assertFreeTierEndpoint`, or to the rating pipeline.

## Frozen contracts touched

None.

## Invariants at risk

- **INV-2** — the shipped replay is re-simulated from its log, never a recording.
- **INV-4** — the thinking budget is metered, never set. The generator uses the existing metering probe.
- **INV-6** — provider and endpoint are logged per call, and are what the Decision Point displays.
- **INV-8** — free tier only. `assertFreeTierEndpoint` is not loosened, and a paid endpoint is a refusal rather than a warning.
- **AD-14 / rating eligibility** — an exhibition is not a ranked result.

## Two regimes

The reasoning panel is **page chrome** and keeps every `docs/DESIGN.md` rule, including the `--tb-warn` text treatment above. No arena code changes here.

## Test plan

- The generator's no-key path, asserted.
- The fixture drives the reasoning panel, the bank HUD and the Parse Failure rendering.
- `ratingEligibility` excludes the exhibition.
- The secret-leak sweep passes over every added file.
- The fixture log validates against `command-log.v2.schema.json` and verifies its hash.

## Exit gate

Two halves, and the story is `awaiting-operator` between them.

**Agent half (this run):** `npm test` green **AND** `bash scripts/audit-invariants.sh` exiting zero **AND** `node scripts/visual-gate.mjs` exiting zero with `reasoning-panel-shows-text` passing against the fixture. Committed. Status set to `awaiting-operator` with the `operator_actions:` above intact.

**Operator half (later):** the key is provided, the generator is run, the real log and sidecar are reviewed and committed, and the story is closed with `bmad-loop confirm 12-12-ship-one-real-llm-vs-llm-replay`.

## Visual check finding

## Standing rule

If this story appears to require a change to anything in `docs/contracts/`, **stop and escalate**. If it appears to require a paid endpoint, a stored key, or a stubbed model response presented as a real one, **stop** — a fabricated LLM replay on a benchmark's front page is worse than no replay at all, and the operator path exists so that it never has to be considered.

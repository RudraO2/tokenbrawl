---
story: "11.4"
epic: E11
status: todo
frs: []
depends_on: ["11.1", "11.2", "10.4"]
---

# Story 11.4 — The Ultimate becomes a cutscene

As a visitor, I want the Ultimate to build, detonate and then fire, so that it
lands like the arcade super it is modelled on rather than like an event that has
already happened.

## Reference material

**Read `docs/DEV-REFERENCE.md` first.** It gives the reference project's
location (`<REF>` below) and the four mechanisms that may never be copied — this
story is where three of the four are most tempting.

| What | Where |
|---|---|
| The cinematic itself | `<REF>/game_source/js/screens.js:2810` — `drawUltimateCinematic` |
| Letterbox + vignette | inside the above, first ~25 lines |
| Growing orb → bloom | inside the above, the `"grow big big BOOM"` block |
| Cinematic timer and the white flash at tick 80 | `screens.js:1494-1507` |
| The `ultimate` event: what fires, in what order | `screens.js:1303-1314` |
| Beam | `screens.js:2108` — `drawBeam`; its window at `screens.js:2213` |
| Assets: per-fighter muzzle / beam / impact | `<REF>/game_assets/atlas/fx_ult.json` + `fx/fx_ult.png` |
| Assets: portraits | `<REF>/game_assets/portraits/<id>.png` |
| **Never copy:** the simulation freeze | `<REF>/game_source/js/core.js:48`, applied at `625` and `1387` |

Copy assets into `apps/web/public/`; **nothing there may name the reference
project**, in a filename or in contents.

## What is wrong with the cinematic we have

Story 10.4 shipped a correct cinematic under the flat-surface rule: a 90-frame
renderer-held freeze, a solid flash on frames 0-3, an accent band that expands
over 22 frames and holds, a streak field, a shake, and an `ULTIMATE` banner.
Every part of it is hash-neutral and seek-safe, and **that machinery is kept.**

What is wrong is the **timing**. Ours is front-loaded — the flash is the first
thing that happens. The reference is back-loaded, and that is why its version
reads as a payoff:

| | Reference | Ours today |
|---|---|---|
| Flash | tick 80, at the end | frames 0-3, at the start |
| Build | orb 6px → 80px on `growP²` across ticks 0-78, then blooms ×3.2 | none |
| Release | 50-tick beam sweeping out on `easeOutCubic` over 8 ticks | band expands 22 frames, holds |
| Framing | letterbox bars in over 15 ticks (`easeOutBack`), vignette | none |
| Subject | painted portrait slides in from the caster's side | text banner |

## Acceptance criteria

**Given** the Ultimate's freeze, **When** it plays, **Then** it runs as three
acts: a **build** during which an energy orb at the caster's hand grows on an
accelerating curve and blooms into a muzzle flash; a **slam**, a full-frame
flash tinted with the caster's aura, near the end of the freeze; and a
**release**, a beam that sweeps out from the caster toward the target.

**Given** the freeze, **When** it opens, **Then** letterbox bars slide in over a
tuned frame count and a vignette darkens the stage, so the frame reads as a
cutscene rather than as a fight with a banner over it.

**Given** the caster, **When** the cinematic draws them, **Then** it draws
**their own** portrait and **their own** `ult_<id>_muzzle` / `_beam` / `_impact`
art in their own aura colour. Per the owner's ruling of 2026-08-07 the Ultimate's
*presentation* is per-character.

**Given** that per-character presentation, **When** the simulation is examined,
**Then** it is untouched: one `special` Action, one cost, one damage number, one
frame window, for every fighter. This story adds art and colour, never a
mechanic — no `packages/env-fighter` change whatsoever.

**Given** a fighter with no portrait or no ult art, **When** they throw it,
**Then** the cinematic degrades to the shared set and then to Story 10.4's
banner-and-band, and nothing throws.

**Given** every duration and extent, **When** they are expressed, **Then** they
are integer frame counts and basis points in the tuning table, never
milliseconds, never a clock, and never a literal at a call site (INV-1, INV-3).

**Given** the timeline scrubber, **When** a visitor seeks across the Ultimate,
**Then** every frame is a pure function of the frame index — scrubbing back and
forth reproduces identical output.

**Given** `prefers-reduced-motion: reduce`, **When** the Ultimate plays,
**Then** the track keeps its shape and frame count, and the flash, the shake and
the moving field are switched off, exactly as Story 10.4 already does.

**Given** any Match, **When** its Final-State Hash is compared before and after,
**Then** it is unchanged (AD-15). **This is the story's central risk and it is
asserted against `spectate-03`**, the one committed Command Log containing an
Ultimate — the demo Match contains none and would pass vacuously.

**Given** the new assets, **When** they land, **Then** `docs/ASSETS.md` records
them **in the same change**.

## Scope

`apps/web/src/render/juice.ts`, `apps/web/src/render/juice-draw.ts`,
`apps/web/public/fx/`, `apps/web/public/portraits/`,
`apps/web/src/startup.ts`, `docs/ASSETS.md`.

## Frozen contracts touched

None.

## Invariants at risk

- **AD-15** — the whole story is a hash-neutrality claim.
- **INV-1 / INV-3** — frames, never time.
- **INV-8** — same-origin static assets.

## Standing rule — translate, do not reimplement

Repeated from Story 10.4 because it is still the likeliest way this goes wrong,
and 11.1 has removed the guard rail that used to make it obvious:

- **The reference's `CINEMATIC_FREEZE = 90` lives inside its `step()`.** It
  freezes the *simulation*. Porting that changes Tick counts, Match length, the
  Decision Point budget and **every Final-State Hash in this repository**. The
  freeze stays on the clock axis, held by the renderer.
- **Do not port the particle pool.** It is stepped and it draws from an
  ungoverned RNG. Both contradict a replay that reproduces.
- **Do not port the wall clocks.** No `setTimeout`, no `performance.now()`, no
  `ctx.currentTime`.

What is fair game is the **behavioural facts**: 90 frames of held picture,
letterbox over ~15 with `easeOutBack`, orb `6→80px` on `growP²` to ~tick 78,
bloom ×3.2 over ticks 66-78, flash at ~80, beam sweeping over 8 with
`easeOutCubic`, impact art at 1.8x the beam scale.

## Test plan

- Final-State Hash identical with the cinematic enabled and disabled, on
  `spectate-03`.
- The same frame index drawn twice yields the same call sequence.
- Seeking backwards across the Ultimate and forwards again reproduces frames.
- A Match with no Ultimate produces an unchanged call sequence.
- Two fighters produce two different cinematics (per-character art is wired, not
  merely present — the Story 9.7 failure).
- A missing portrait falls back without throwing.

## Exit gate

`npm test` green **AND** `bash scripts/audit-invariants.sh` exiting zero **AND**
the hash-neutrality test passing **AND** `docs/ASSETS.md` updated in the same
change **AND** Story 10.1's visual gate with the Ultimate captured **in all
three acts** — build, slam, release — and the finding recorded here.

## Visual gate (Story 10.1) — run 2026-08-08

**How the state was reached.** The demo Match contains no Ultimate, so the
replay player was temporarily pointed at `spectate-03` and
`prefersReducedMotion` temporarily forced to `false` — the headless browser
reports `prefers-reduced-motion: reduce`, which pins the whole cinematic and
would have made every capture the accessibility path. Both edits were reverted
before the commit and `git status` is clean of them. The clock was frozen by
starving `requestAnimationFrame`, because a seek is otherwise overwritten by
the next autoplay frame. Canvas captured at 1:1 (`style.width = '960px'`).

The Ultimate is at clock frame **401** of 630; caster is agent 0 (`clawde`),
connected.

**Replay player — the three acts, each captured and probed.**

| Act | Frame | What the pixels showed |
|---|---|---|
| Build | 460 (age 59) | Letterbox 52px top and bottom, vignette, `clawde`'s own portrait with an aura frame, `CLAWDE` in arcade type with its offset shadow, the orb at the hand. Aura wash probed across the stage: `#0d0b14` at x=60, `#462e26` at x=200, `#e3bb93` at x=300 — a falloff, not a block. |
| Slam | 482 (age 81) | 81 982 px of `#d8b58c` and 71 947 of `#d9b68e` out of 384 000 — the whole frame washed in the aura-tinted white. The letterbox is already 0 here: the flash covers the cut back to gameplay, as the reference intends. |
| Release | 505 (age 104) | Letterbox gone, HUD back (`#080a10` plate, `#5a6480` frame, health greens). Beam on its axis row y=252: 760 lit pixels from x=158 to x=959 — `#ffff58` at the caster, `#ffb433` mid-beam, `#fff381` at the far edge. Impact burst at the opponent, streak field in the caster's aura. |

**Two easings verified in real pixels, not in the record.** The letterbox
ramped 0 → 26 → 36 → 44 → 49 → 53 → 55 → **57** → 56 → 55 → 54 → 53 → **52**:
the `easeOutBack` overshoot is five pixels above the settled height and is
visible. The slam's centre pixel ran `#f0c99b` → `#d8b58c` → … → `#201c18` →
gone across ten frames: one rise, one fall, no strobe.

**Reduced motion verified live**, by accident and then on purpose: the first
pass ran under the browser's own `prefers-reduced-motion: reduce` and showed
the letterbox pinned at 52 from age 1, no flash at any frame in the slam
window, and no streak field — the same beat, standing still.

**One defect found and fixed here.** The aura glow behind the portrait was
drawn as three nested squares with the widest at 60% opacity: a hard-edged
480px orange block covering half the fight, with two smaller blocks inside it.
Every call-sequence assertion passed. Replaced with eight steps at 9–30%, and
the radius re-derived from the stage's *height* (the reference's 360 of 1080)
rather than a quarter of its width. Both the ramp's direction and its step
count are now asserted.

**Arcade and Spectate**, screenshotted as the epic requires even though this
story names neither: both unbroken. Spectate plays `spectate-02` with the
arcade HUD, both fighters distinct, backdrop and floor correct, and no
cinematic — it has no juice layer until Story 11.6. Arcade started a Match and
its controls are intact.

**Console**, filtered to error and warn: no error, and the only two warnings
are mine — the `link preload` notice caused by temporarily redirecting the demo
log, and the `willReadFrequently` notice caused by my own `getImageData`
probes. **No "Ultimate FX unavailable" and no "Impact FX unavailable"**, so the
atlas and both portraits fetched and decoded.

**Observation, not fixed.** The eight-step glow still bands faintly into
rectangles at its edges. That is the floor of what this `Canvas2D` port can do
— it has no `createRadialGradient` — and widening the port is not this story's
job. Recorded in `deferred-work.md`.

## Standing rule

If this story appears to require a change to anything in `docs/contracts/`,
**stop and escalate**.
